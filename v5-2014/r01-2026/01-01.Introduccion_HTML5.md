# Capítulo 01: Introducción a HTML5

## La Historia del HTML5

La idea básica detrás de HTML es que utilices elementos que estructuren tu contenido

> 💡 **TIP**: Puedes leer la documentación oficila de la versión W3C del estándar HTML5 en: [www.w3.org/TR/html5](www.w3.org/TR/html5)

De las mayores novedades es la inclusión del "almacenamiento local"

## Tu primer ejemplo HTML5

```html 001_ejemplo.html
<!DOCTYPE html>
<title>Un Documento HTML muy Diminuto</title>
<p>Vamos navegador, muéstrame el estilo del HTML5.</p>
```

Puedes omitir la etiqueta `<title></title>` si por ejemplo lo envías como email. Pones el título como asunto del email y lo demás como parte del email

```html 002_ejemplo.html
<!DOCTYPE html>
<html>
<head>
    <title>Un Documento HTML muy Diminuto</title>
</head>
<body>
    <p>Vamos navegador, muéstrame el estilo del HTML5.</p>
</body>
</html>
```

Aquí la etiqueta `<html></html>` es completamente opcional para los navegadores modernos

### El elemento _Doctype_ del HTML5

La primer línea de todo el documento HTML5 es un código especial llamado `doctype`. El _doctype_ claramente indica el estándar que será utilizado para escribir el marcado del documento que sigue. Así es como le anuncias a la página que se adhiera al estándar HTML5:

`<!DOCTYPE html>`

No se necesita especificar la versión, sabe que es un documento HTML.

### Codificación de caracteres

Es el estándar que le dice a tu computadora como convertir tu texto en una secuencia de bytes cuando estos están almacenados en un archivo. Actualmente se opta por el estándar UTF-8, el cual es compacto, rápido y soporta todos los caracteres no-Inglés que puedas necesitar

```html
<head>
    <meta charset="utf-8">
    <title>Un Documento HTML Muy Pequeñito</title>
</head>
```

### El idioma

Para el inglés basta con _en_ para Inglés normal, pero para otros idiomas busca su código.
`<html lang="ll-CC">`, donde "ll" es el idioma y "CC" es el país/region

- `en` Inglés
- `es` Español

Combinación de idioma con país

- `en-US` Inglés (Estados Unidos)
- `es-MX` Español (México)

### Añadiendo una hoja de estilo

Casi todo sitio profesional utiliza hojas de estilo CSS. Para especificar cual quieres utilizar, lo haces añadiendo el elemento `<link>` a la sección `<head>` del documento HTML5, así:

```html
<head>
    <meta charset="utf-8">
    <title>Un Documento HTML Muy pequeño</title>
    <link href="style.css" rel="stylesheet">
</head>
```

### Añadiendo JavaScript

```html
<head>
    <meta charset="utf-8">
    <title>Un Documento HTML Muy pequeño</title>
    <script src="script.js"></script>
</head>
```

## Un mirada de cerca de la sintaxis de HTML5

HTML omite las etiquetas de cerrado de elementos vacíos, son las etiquetas que no pueden anidar contenido, como `<img>`, `<br>` y `<hr>`. Estas etiquetas puedes escribirlas de las siguientes tres formas:

- `<br />` - con espacio y slash.
- `<br>` - sin slash.
- `<br/>` - sin espacio y con slash.

### Recomendaciones

- Utiliza las etiquetas en minúsculas
- Utiliza doble comilla para los valores de atributos de las etiquetas

> 📝 **NOTA:** Validador html oficial [https://validator.w3.org/](https://validator.w3.org/), puedes validar una url, subir un archivo o pegar el código.

## Familia de elementos de HTML5

TODO: pág:24